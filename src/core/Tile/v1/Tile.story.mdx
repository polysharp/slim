import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Grid from '../../Grid';
import Tile from './Tile.jsx';

<Meta title="Atoms/Layout" component={Tile} />

# Overview

<Preview withToolbar>
  <Story name="Tile">
    <Grid rows={['auto', 'auto', 'auto', 'auto', 'auto']}>
      <Tile rowStart={['auto', 'auto', 'auto', 'auto', 6]} rows={[1, 1, 1, 1, 2]}>
        <div style={{ height: '200px', with: '200px', border: '1px solid #000' }} />
      </Tile>
    </Grid>
  </Story>
</Preview>

<Props of={Tile} />

## Usage

```jsx
import Tile from '@polysharp/slim/core/Tile';
// or
import { Tile } from '@polysharp/slim';

<Grid>
  <Tile />
  <Tile />
  <Tile />
<Grid/>
```

## Settings

Theme properties can be view with `useTheme()` hook.

| Theme Prop | Default | Description |
| ---------- | ------- | ----------- |

